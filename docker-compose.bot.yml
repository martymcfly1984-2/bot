version: '3.8'

services:
  alchemy_pro_bot:
    build: .
    environment:
      - ENGINE_URL=ws://engine:8081/ws
      - BOT_ID=Top_of_the_Bot_AK
      - BOT_ICON=🧙
    networks:
      - alchemy-net
    restart: on-failure

networks:
  alchemy-net:
    external: true